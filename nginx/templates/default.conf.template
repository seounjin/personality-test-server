
server {
     listen 80;
     server_name ${SERVER_DOMAIN} www.${SERVER_DOMAIN};
     server_tokens off;

     location /.well-known/acme-challenge/ {
             allow all;
             root /var/www/certbot;
     } 

}
